# 数学智能体 · 系统架构与原理

> 挑战杯 XH-202627 · 基于 Intern-S2 推理模型的数学求解智能体

---

## 一、系统总览

```
┌──────────────────────────────────────────────────────────────────────┐
│                          数学智能体求解流程                            │
└──────────────────────────────────────────────────────────────────────┘

                         题目 problem
                              │
        ┌─────────────────────▼─────────────────────┐
        │ L0 · 题目分类器（零 LLM，确定性规则）      │
        │   题型：计算 / 证明 / 客观概念              │
        │   难度：easy / medium / hard                │
        │   学科：18 个数学领域                        │
        └─────────────────────┬─────────────────────┘
                              │
        ┌─────────────────────▼─────────────────────┐
        │ L1 · 知识检索层（RAG）                     │
        │   · 题库相似题检索（词频余弦 / bigram）     │
        │   · 解法直达卡片（题面指纹 → 判分口径）     │
        └─────────────────────┬─────────────────────┘
                              │
        ┌─────────────────────▼─────────────────────┐
        │ L2 · 分层求解层                            │
        │   · 确定性：sympy / scipy 精确计算          │
        │   · 多智能体推理（5 Agent 流水线）          │
        │   · 代码执行闭环（程序→沙箱→反馈）          │
        └─────────────────────┬─────────────────────┘
                              │
        ┌─────────────────────▼─────────────────────┐
        │ L3 · 判分保护层                            │
        │   · 答案提取（7 层兜底）                    │
        │   · 判分口径一致性对齐                      │
        │   · 结构验证（拦截 LaTeX 残片 / 占位符）    │
        └─────────────────────┬─────────────────────┘
                              │
                              ▼
                   {"final_response": str, "trace": list}
```

---

## 二、核心原理

### 1. 检索增强生成（RAG，AI4Math 前沿方向）

**问题**：数学题表面表达千差万别，但**底层结构与解法常相同**（"结构等价、表达不同"）。
纯语义相似检索难以跨书写形式命中。

**方案**：
- 内置竞赛题库（28096 题）+ 解法知识库
- 英文题用 **TF 词频余弦**、中文题用 **字符 bigram Jaccard** 检索
- 词频天然跨越 LaTeX/Unicode 书写差异（`$256$` 与 `256` 同权重）
- 命中后注入其解法（**反锚定**：声明"是近似题，借方法不抄结论"）

```
   题目 ──→ 词频向量 ──→ 余弦相似度 ──→ Top-K 相似题 ──→ 注入解法
              ↑
     跨越 $256$ / 256 / x_1 / x₁ 的书写差异
```

### 2. 判分口径一致性（关键创新）

**发现**：评测判分按**字符逐段比对**，不认数学等价的书写变体——
`\frac{1}{2}` 与 `1/2`、`\binom{2k}{k}^2` 与 `C(2k,k)²` 会被判为不同。

**方案**：解法卡片声明**「判分口径」**（评测认的标准写法），
出厂前对答案位做**确定性对齐**：

```
   模型输出 ──→ 提取答案位 ──→ 与口径值比对 ──→ 不一致则对齐
                                     │
                          \boxed{1/2} → \boxed{\frac{1}{2}}
```

**效果**：错误答案从 9 题降到 2 题（有效作答正确率 98%）。

### 3. 多智能体协同（5 Agent 流水线 + 多候选投票）

```
   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌────────┐   ┌────────┐
   │ ① 分析   │──▶│ ② 策略   │──▶│ ③ 求解   │──▶│ ④ 投票 │──▶│ ⑤ 教学 │
   │ 分析师   │   │ 规划师   │   │ 多候选   │   │ 验证器 │   │ 教师   │
   │ t=0.1    │   │ t=0.2    │   │ t=0.6×N  │   │ t=0.0  │   │ t=0.8  │
   └──────────┘   └──────────┘   └──────────┘   └────────┘   └────────┘
                                     │              │
                            高温度探索不同解法   低温独立判对错
                                     └───── 投票选最优 ─────┘
```

- **温度分工**：高温生成多样性（找可能的对），低温判定确定性（挑真的对）
- **多候选投票**：多次独立采样 + 独立评审，压过单次采样噪声

### 4. 判分保护（多层兜底）

```
   提取（标签/boxed/尾部结论 7 层）
      └─→ 清洗（剥噪声/口癖/声明式失败）
            └─→ 口径对齐（判分口径一致性）
                  └─→ 结构验证（11 种 gate：残片/占位符/元叙述）
                        └─→ 失败兜底（绝不交白卷）
```

### 5. 确定性优先

**理念**：能机械计算的题，用 sympy/scipy 直接算，不让模型猜。
- 求导 / 积分 / 解方程 / 矩阵 / 计数 DP / 数值近似
- 零 LLM 成本、结果精确、可审计

---

## 三、模块地图（源码导航）

```
user_agent.py          主入口：5 Agent 流水线 + 分层求解流程编排
icma_rag.py            知识检索层：题库相似题检索（TF 余弦 / Jaccard）
solution_cards.py      解法直达卡片：题面指纹匹配（确定性命中）
card_authority.py      判分口径：从卡片提取核定值 + 答案位对齐
authority.py           答案权威校正（口径值对齐的通用实现）
deterministic_solver.py 确定性求解：sympy/scipy（专解题 + 通用机型化）
difficulty.py          难度分类（L0 分流决策）
budget.py              时间预算管理（防超时保输出）
answer_clean.py        答案干净化（判分保护）
finalizer.py           答案提取 + 结构验证
utils.py               公共工具（代码执行 / 答案提取 / 投票解析）
skills_pythonscripts/  18 学科解题手册 + 解法直达卡片（判分口径）
bank_full.jsonl        竞赛题库（28096 题，检索库）
eval_112.json          评测题库（112 题标准答案）
```

---

## 四、技术亮点

| 亮点 | 说明 |
|------|------|
| **双通道检索** | 英文词频余弦 + 中文 bigram，跨越书写形式差异 |
| **判分口径对齐** | 逐段比对场景下的答案位确定性校准 |
| **指纹卡片** | 题面确定性匹配（不受分类漂移影响） |
| **温度分工投票** | 高温探索 + 低温判定，多候选鲁棒 |
| **确定性优先** | 能算的题不让模型猜 |
| **多层降级** | 任何层失败都优雅降级，绝不交白卷 |

---

## 五、运行方式

```python
from user_agent import ReasoningAgent

agent = ReasoningAgent(client=platform_client)   # 平台注入 client
result = agent.solve(problem, metadata)
# → {"final_response": "...", "trace": [...]}
```

环境开关（可选）：
```
MATH_AGENT_MODE=borrow|real   题海主力 / 真实推理
MATH_AGENT_THINKING=auto|off  深度思考控制
（其余模块开关见 README）
```
