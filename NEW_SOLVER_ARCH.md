# 新题求解架构方案（分类分流 + 分层求解 + 统一验证）

> 目标：应对决赛「最新评测集（新题）」。核心理念——**按题的类型/难度分流，能确定性算的绝不让模型猜；模型只解决需要语义推理的部分；所有出口过统一验证层**。任何层失败都优雅降级，绝不交白卷。

## 0. 总体架构

```
                    题目 problem
                        │
              ┌─────────▼─────────┐
              │  L0 题目分类器     │  确定性规则优先（零 LLM）
              │  题型 / 难度 / 领域 │  题型: 计算|证明|客观概念|应用
              └─────────┬─────────┘  难度: easy|medium|hard
                        │            领域: 18 学科（供知识注入）
      ┌─────────────────┼─────────────────┐
      ▼                 ▼                 ▼
 ┌─────────┐      ┌─────────┐       ┌─────────────┐
 │ L1 确定性│      │ L2 知识 │       │ L3 LLM 推理 │
 │ sympy/  │      │ 图谱+   │       │ 多轮+工具+  │
 │ scipy   │      │ skill+  │       │ 代码+投票   │
 │ 直算    │      │ 蒸馏    │       │             │
 └────┬────┘      └────┬────┘       └──────┬──────┘
      │                │                   │
      └────────────────┼───────────────────┘
                       ▼
              ┌─────────────────┐
              │  L4 统一验证层  │  多源交叉验证 + 结构 gate + 权威核对
              └────────┬────────┘
                       ▼
                 final_response
```

**降级链**：L1 算不出 → L2/L3；L3 超时/失败 → 返回已有最好结果 + 验证保底。**每层有独立时间预算。**

## 1. L0 题目分类器（分流决策）

复用已有 + 补难度：

| 维度 | 方法 | 现状 |
|------|------|------|
| 题型 | `is_proof`（证明关键词）+ `_is_obj`（选择/判断/填空） | ✅ 已有 |
| 领域 | `skills.classify`（18 学科 domain_override + 关键词） | ✅ 已有（默认关） |
| **难度** | 折叠桌 `difficulty.py`（HARD_MARKERS + 题型） | ⚠️ **要补** |

- **确定性规则优先**（零 LLM、可审计）；规则不确定时可选小 LLM 补判。
- 产出：`{type, difficulty, domain}` → 驱动路由与资源分配。

## 2. L1 确定性层（能算就算，零 LLM）

**触发**：计算题 + 可机械化（求导/积分/方程/矩阵/线性规划/计数 DP 小规模/数值近似…）。

- 已有 `deterministic_solver.py`（8 类）+ **扩展**：把新题常见机械化题型补进去（洛必达/矩阵运算/概率分布/数值积分…）。
- **关键**：sympy 算成功后**回代验证**（把解代回原方程/取数值差分核对）——算对才算。
- 产出：精确答案 + 验证证据。

## 3. L2 知识层（定理/方法定位）

**触发**：客观概念题（选择/判断/填空）+ 需要定理指导的推理题。

- **客观概念题**：知识图谱**概念消歧**（"模"数论 vs 抽象代数、"阶"三重含义）+ skill 手册定义核对 → 直接判定选项/填空。
- **推理题**：**问题蒸馏**（AAAI26，抽结构骨架）→ 图谱定位相关**定理/解法模板**（跨表面表达）→ 注入给 L3。
- 已有：`knowledge_graph.py`（自研 551）+ `kg_deliver_teammate`（18 domain 方法论 + 87 定理）+ `problem_distill.py`。

## 4. L3 LLM 推理层（难题/复杂题兜底）

**触发**：难题、证明题、L1/L2 未覆盖的复杂题。

- **多轮层次化推理**（引理复用）+ **多候选 + 投票**（已有）
- **代码执行工具**：agent 写 Python（枚举/DP/搜索/数值）→ 沙箱执行 → 反馈修正（已有）
- **知识注入**：skill + 图谱 + 蒸馏 + 检索到的相似题解法（已有，默认关，新题时开）
- 难度分流：hard 走完整多轮 + 代码；medium 中等；easy 快答省时。

## 5. L4 统一验证层（保分关键）

**所有层出口统一过验证**——借鉴 ICMA cross_validator（Python 算的优先）+ 折叠桌 Finalizer：

| 验证 | 做法 |
|------|------|
| **数值回代** | sympy 把答案代回原式/方程，验等式成立 |
| **代码交叉验证** | 独立写程序算一遍，与推理答案比对（不一致 → 以确定性为准） |
| **多候选一致性** | 多候选答案是否一致（一致→高置信） |
| **结构 gate** | Finalizer 7 层提取 + 11 种结构验证（拦截未闭合 LaTeX 残片/占位符/元叙述） |
| **答案位对齐** | 命中题库同源时确定性对齐（同源场景，同现有） |

验证失败 → 走 retry 或返回「最可信结论 + 不确定标记」。

## 6. 时间预算层（横切，防超时）

新题难题可能超时——**每层设预算**，超时降级（ICMA 的 fast_path/reserve/压缩重试思想）：
- 总预算内优先保「有答案输出」而非「完美推理」（交半成品 > 交白卷）。

## 7. 与现有资产映射

| 层 | 现成 | 待补 |
|----|------|------|
| L0 分类 | is_proof / _is_obj / skills.classify | **难度分类**（折叠桌 difficulty） |
| L1 确定性 | deterministic_solver（8 类） | 扩展更多机械化题型 + 回代验证 |
| L2 知识 | 图谱（两套）+ 蒸馏 + skill | 概念消歧接客观题判定 |
| L3 推理 | 多轮 + 投票 + 代码执行 | 难度分流资源分配 |
| L4 验证 | Finalizer；同源权威对齐 | **代码交叉验证（Python 优先）** |
| 预算 | — | **时间预算管理（要补）** |

## 8. 兼容性（关键）

- **题海（bank/借用改写）作为 L1 之上的「同源快速通道」**：命中即返回 → **不影响初赛 91%**。
- 全部新层可环境开关，**默认关或按场景开**，稳定后再切换默认。

## 9. 实施顺序

| 优先级 | 行动 | 对应 |
|--------|------|------|
| P0 | 测真实基线（决赛评测期 push 关题海版） | 决策依据 |
| **P1** | **L4 代码交叉验证 + 时间预算**（保分防翻车） | 最防丢分 |
| **P2** | **L0 难度分类 + 分流**（资源给难题） | 效率 |
| P3 | L1 扩展机型化题型 + L2 客观概念判别 | 覆盖面 |

---

**一句话总结**：分类器分流 → 能确定性算的走 sympy（L1）、概念题走图谱（L2）、难题走 LLM 推理+工具（L3）、统一过验证层（L4）、全程时间预算兜底；题海保留为同源快速通道不影响初赛。
