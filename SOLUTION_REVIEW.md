# 正确率提升方案 — 价值评估

> 评估来源：`用户现在想要一个汇总的方案来提(1).docx`（内部分析文档）  
> 评估基准：官方评测 `dacf4d1` 版本 19.6%（22/112）  
> 评估日期：2026-08-09

---

## 一、方案全景

该方案提出 20+ 个子方案，分 4 个阶段：

| 阶段 | 方案数 | 预期累计正确率 | 核心逻辑 |
|------|--------|--------------|---------|
| 阶段一（地基级） | 4 | 44~62% | 解决最大的几个短板 |
| 阶段二（加固级） | 4 | 52~67% | 审题+验证+多方法 |
| 阶段三（精细化） | 7 | 55~70% | 细分题型+双验证+自对弈 |
| 阶段四（兜底） | 3 | +5~10% | 语言兜底+SymPy主动调用 |

---

## 二、与我们已经完成的工作对比

| 方案编号 | 方案内容 | 预期收益 | 我们已完成？ |
|---------|---------|---------|:--:|
| P0-A | 多层强制中文输出 | +10~15% | ✅ 已做 |
| P0-B | max_tokens 扩容 | +3~5% | ✅ 已做 |
| 5A | 温度梯度 | +2~3% | ✅ 已做 |
| S1 | 策略结构化 JSON + 领域模板库 | +8~12% | ❌ |
| S2 | 步骤级验证 + 策略重试 | +6~10% | ❌ |

**我们的版本（9e33d89）已覆盖该方案 P0-A + P0-B + 5A，预期正确率 28-32%。**

---

## 三、各方案逐项评估

### 🥇 高价值方案

#### S1：策略结构化 JSON + 领域模板库（+8~12%）

**价值评估**：⭐⭐⭐⭐⭐ 最高

当前策略师输出自然语言（如"可以考虑特征线法"），缺乏约束。该方案要求输出严格 JSON，含 `mandatory_steps`（必做步骤）、`key_theorems`（需引用的定理）、`validation_plan`（验证方案）、`fallback_method`（备选方法）。

**为什么有效**：
- 杜绝策略师跳过关键步骤
- 求解器可以按骨架强制填充
- 失败时可以 fallback_method 换策略重跑

**风险/成本**：
- 需要设计 18 个领域的求解模板，约 150 行 JSON
- 策略师必须正确输出 JSON 格式，需要 prompt 优化

**建议**：等现有评测结果确认中文+max_tokens 有效后优先启动。MVP 可先只做 5 个核心领域。

---

#### S2：步骤级验证 + 策略重试（+6~10%）

**价值评估**：⭐⭐⭐⭐

目前代码修正只做整体"执行结果反馈"。该方案要求求解按 step 逐个生成→每步验证→失败修正→连续失败触发 fallback_method 换方法。

**为什么有效**：
- 在错误传播前就阻断
- 可以定位具体哪一步出错

**风险/成本**：
- 需要重构 `_generate_candidates()` 的核心循环
- 验证逻辑需要区分代码结果验证/定理引用验证/逻辑一致性验证
- 容易引入 bug

**建议**：S1 完成后再做（依赖 S1 的策略 JSON 骨架）。

---

### 🥈 中等价值方案

#### 3A：审题锁定 + 子问题确认（+4~6%）

当前 Analyzer Agent 已做题型分类。该方案要求更精确的审题 JSON（`problem_restatement`、`sub_questions`、`given_conditions`、`final_goals`）。

**评价**：功能已有，提升有限。

---

#### S3：验证策略真执行闭环（+4~7%）

选 best 候选后，专门调一个「验证执行 Agent」按 `validation_plan` 写代码真的去代入原方程/数值近似。

**评价**：需要新增 Agent + API 调用，性价比中等。

---

#### S4：多方法差异化候选（+3~5%）

策略师出 3 种方法，候选按"2 方法 × 2 温度 + 1 数值验证"分配。

**评价**：依赖 S1，独立做不了。

---

### 🥉 低价值方案

| 方案 | 预期收益 | 为什么不值得 |
|------|---------|------------|
| 4A 证明题 6 类细分 | +3~5% | 细分过多，每类样本量小 |
| S5 前置计算共享 | +2~4% | 边际收益，增加复杂度 |
| S6 难度自适应 | +1~3% | 基础问题没解决前不应微调 |
| 6A/B 数值解析双验证 | +4~8% | 针对少数题有效 |
| P0-C 语言检测兜底 | +1~2% | 已做强制中文，不需要 |
| 5B 自我对弈 Critique | +3~5% | 增加大量 API 调用 |
| 2C SymPy 主动调用 | +3~5% | 工程量大，通用性差 |
| 3B 候选级跑偏检测 | +2~3% | 我们已有跑偏检测 |

---

## 四、推荐执行路线

```
当前 (9e33d89) 预期: 28-32%  ← 等待评测验证
     │
     ├── 评测 > 25% → 启动 S1 MVP (5 核心领域模板)
     │                  预期: +8~12% → 36-44%
     │
     └── S1 有效 → 启动 S2 MVP (步骤代码验证)
                    预期: +6~10% → 42-54%
```

---

## 五、最终结论

1. **该方案的核心逻辑是正确的**，与我们独立分析的结论完全一致（P0-A/P0-B 就是强制中文+max_tokens）
2. **方案已有主观执行力**，但预估过于乐观（阶段一预期 44-62% 不现实，实际更接近 35-40%）
3. **S1 策略 JSON + 模板库是最大单项收益**，也是该方案中最有价值的原创思路
4. **其余 15+ 方案性价比低**，在当前阶段不值得分散精力
5. **推荐仅采纳 S1 MVP**：5 个核心领域模板 + JSON 结构化策略输出，改动量可控，收益明确
